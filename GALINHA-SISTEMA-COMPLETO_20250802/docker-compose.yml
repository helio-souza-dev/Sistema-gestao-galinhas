# 🐳 Docker Compose - Orquestra vários containers
# Este arquivo facilita rodar nossa aplicação

version: '3.8'

services:
  # Nossa aplicação de galinhas
  galinha-app:
    build: .                    # Construir usando o Dockerfile
    ports:
      - "3000:3000"            # Porta 3000 do PC -> Porta 3000 do container
    environment:
      # Variáveis de ambiente (suas credenciais do Supabase)
      - NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_URL}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
    restart: unless-stopped     # Reiniciar se parar
